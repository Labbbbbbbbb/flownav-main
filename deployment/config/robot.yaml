# ~/flownav-main/deployment/config/robot.yaml

# 基础运动参数
max_v: 0.1            # 最大线速度 (根据你的 Scout 实际能力调整)
max_w: 0.1            # 最大角速度
frame_rate: 10        # 频率

# 话题名称适配
vel_navi_topic: "/cmd_vel"  # 添加这一行，确保与 pd_controller.py 里的 KeyError 对应